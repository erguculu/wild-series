{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
<h2>Catégories</h2>
{% for category in categories %}
<p><a href="{{ path('category_show', { categoryName: category.name }) }}">{{ loop.index }} / {{ category.name }}</a></p>
{% else %}
<p>Pas de catégories à afficher.</p>
{% endfor %}
{% endblock %}